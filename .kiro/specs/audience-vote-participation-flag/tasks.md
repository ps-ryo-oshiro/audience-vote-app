# Implementation Plan

> **2026-09-10 改訂2で再生成**
> 旧タスク（CSV取込のupsert化・匿名認証・localStorage モードでの結合確認など）は、design.md の方針変更により廃止した。既存コミット（`265944c`・`fb06b4a`）のうち、参加切替UIの骨格・投票画面の絞り込み・Workerの検証と保存は、各タスクで改修のベースとして使う。投票画面はデザイン刷新版（`04dce0f`）をベースにする。
>
> **外部の前提（未解決）**: 本戦12チームのエントリーNo一覧（タスク4.3）、デプロイ先の Cloudflare アカウント（タスク5.1）。
> **並行作業**: 投票画面（`index.html`・`app.js`・`vote.css`）のデザイン刷新は、別セッションが `6d853c4` で完了した（タスク3.1に着手できる）。作業ブランチは両セッションとも `feature/vote-ui-redesign`。コミットは自分が変更したファイルだけをパス指定で行う。
>
> **共通のデモデータ**（タスク3.1・3.2で同じ内容にする。両画面は、デモモードではブラウザ内の同じ `teams` データを読み書きする）:
> | ID | No | 部門 | 区分 | 表示 | 動画URL | 確認する点 |
> |----|----|------|------|------|---------|------------|
> | entry-01 | 1 | life | 本戦 | true | あり | 本戦の通常表示 |
> | entry-02 | 2 | life | 候補 | false | あり | 非表示の候補 |
> | entry-03 | 3 | work | 本戦 | true | あり | 本戦の通常表示 |
> | entry-04 | 4 | work | 候補 | true | あり | 表示中の候補 |
> | entry-05 | 5 | local | 本戦 | **false** | あり | 本戦は表示状態が false でも表示される（1.2） |
> | entry-06 | 6 | local | 候補 | false | **なし** | 動画URLなし（4.4） |

- [ ] 1. 基盤: 投票アプリ専用DBへの接続と、配信・実行環境の準備
- [x] 1.1 接続設定を git 管理外にし、ひな形を用意して専用DBの接続値を入れる
  - 接続設定ファイルをバージョン管理から外し、接続設定ファイルとローカル用の環境変数ファイルを管理対象外に指定する
  - 項目名とプレースホルダーだけのひな形（入室ID・パスワードの項目を含む）をリポジトリに追加する
  - 手元の接続設定に `audience-vote-2026` の接続値を入れる（入室ID・パスワードは保留中のため初期値のまま）
  - 完了状態: リポジトリの追跡対象に接続設定ファイルが含まれず、ひな形が含まれる。接続設定を一時的に外して簡易サーバーで起動すると、投票画面がデモモードで表示される
  - _Requirements: 11.1, 11.2, 11.5_

- [x] 1.2 投票アプリ専用DBのセキュリティルールを定義し、反映する
  - 前提確認: Firebase CLI のプロジェクト一覧に `audience-vote-2026` が出る（オーナーとしてログイン済み）
  - 投票アプリの領域以外を拒否し、チームと受付設定は読み書き可、票は読み取り可・新規追加のみ可とするルールに置き換える
  - CLI でルールを反映するための設定を追加し、専用DBに反映する
  - 完了状態: 認証なしのリクエストで、チーム一覧の読み取りと票の新規追加が成功し、票の削除・既存票の上書き・投票アプリ領域外の読み取りが拒否される（確認用に作った票は、オーナー権限で削除する）
  - _Requirements: 8.1, 8.2, 10.1, 10.5_

- [x] 1.3 Worker をローカルで実DBにつないで動かせるようにする
  - Worker の設定に、二重投票防止用のKV binding を追加する（本番の namespace ID はタスク5.1で差し替える）
  - ローカル用の環境変数ファイルに、専用DBのURLを設定する
  - 完了状態: Worker をローカルで起動すると、KV binding と DB URL の変数が有効になっていることが起動ログで確認できる
  - _Requirements: 7.2, 11.4_

- [x] 1.4 配信するファイルを許可リストで絞り、SPA の設定を外す
  - 画面に必要な7ファイル（投票画面・管理画面の HTML と JS、2つのスタイル、接続設定）だけを配信対象にする
  - 存在しないパスに `index.html` を返す SPA の設定を外す
  - 完了状態: ローカル起動した Worker で、`/`・`/index.html`・`/admin.html`・`/vote.css`・`/style.css` が200になり、README・Worker本体・Worker設定・ルール・docs・scripts・spec が404になる
  - _Requirements: 10.2_

- [ ] 2. チームの登録スクリプト
- [x] 2.1 (P) 審査アプリのチーム一覧から、初期登録用のパッチを生成する
  - 審査アプリのチーム一覧（読み取りのみで取得したもの）、本戦のエントリーNo一覧、現在のチーム一覧を入力に、エントリーNoから決めたチームIDごとに、項目単位のパッチを出力する
  - 新規チームには全項目（エントリーNo・アプリ名・部門・区分・表示状態・空の動画URL）を、既存チームには区分と本戦の表示状態だけを出力する。本戦から外れた既存チームには、区分と非表示を出力し、そのNoを知らせる
  - 部門が対応しないチームは出力せずに一覧で示す。存在しない本戦Noがあれば、何も出力せずに異常終了する。本戦の件数が12でなければ警告する
  - 審査アプリのプロジェクトIDはスクリプトに書かず、実行時に渡す
  - 完了状態: サンプル入力に対して、空のDBなら本戦が表示・候補が非表示で出力される。候補を1件表示にした現在一覧を渡すと、そのチームの表示状態・動画URL・名称がパッチに含まれない。本戦から外したチームには非表示が出力される
  - _Requirements: 1.1, 1.3, 2.1, 2.2, 2.3, 2.5_
  - _Boundary: Team Patch Script_

- [x] 2.2 動画URLの追記と名称の修正のパッチを生成する
  - エントリーNoまたはアプリ名とURLの一覧から、一致するチームの動画URLだけのパッチを出力する。一致しない行とURLでない行は出力せずに一覧で示す
  - 指定したエントリーNoのチームについて、名称だけのパッチを出力する。該当がなければ異常終了する
  - 完了状態: No 版とアプリ名版の両方のサンプル一覧で動画URLだけが出力され、名称の修正では名称だけが出力される
  - _Requirements: 12.1, 12.2, 12.3, 12.4_
  - _Boundary: Team Patch Script_

- [ ] 3. 画面と投票APIの改修
- [ ] 3.1 (P) 投票画面を「本戦は常に表示・No付き・No順」にし、接続を持ち続けないようにする
  - 前提: デザイン刷新のセッションの作業が終わっている（2026-09-10 に `6d853c4` で完了の連絡あり。デザイン刷新版の見た目とデータ層の境界は維持する）
  - レイアウト上の制約（デザイン刷新のセッションからの申し送り）: 「No.X 名称」はカードのタイトル要素（`.entry__title`）の中に入れる。VIDEO リンクを出さないカードでも、下段の要素（`.entry__foot`。SELECTED 表示が入っている）は残す
  - 本戦と表示ONの候補だけを表示し、部門ごとにエントリーNo順で「No.X 名称」と表示する
  - 動画URLが http(s) でないとき（未設定を含む）は、VIDEO リンクを出さない
  - 票は読み込まず、チーム一覧と受付状態を読み込んだ後にDBとの接続を切る。読み込みに失敗したら、再読み込みを促す
  - デモ用のチームデータを、冒頭の「共通のデモデータ」に置き換える
  - 完了状態: ブラウザ内の保存データを消した状態（またはプライベートウィンドウ）のデモモードで entry-01・03・04・05 がNo順・No付きで表示され、entry-05 は表示状態が false でも表示され、entry-02・06 は表示されない。専用DBにつないだ状態で、読み込み後にDBとの接続が切れていることを開発者ツールで確認できる
  - _Requirements: 1.2, 1.4, 4.1, 4.2, 4.3, 4.4, 4.5, 4.6, 4.7, 8.3, 11.5, 13.1, 13.3_
  - _Boundary: Voting Page_
  - _Depends: 1.1, 1.2_

- [ ] 3.2 (P) 管理画面の取込と認証を削除し、敗者復活候補の表示切替パネルを作る
  - ファイル取込の画面・処理・ライブラリの読み込みと、匿名認証の処理・ライブラリの読み込みを削除する
  - 集計とは別の「表示切替」パネルに、本戦の一覧（No順・「常に表示」・切替なし）と、敗者復活候補の一覧（No順・チェックボックス）を分けて示す。名称はエスケープし、部門は日本語で表示する
  - 表示中の候補の数を示し、切替のたびに更新する。保存に失敗したら切替を元に戻し、エラーを表示する
  - デモ用のチームデータを、冒頭の「共通のデモデータ」に置き換える
  - 完了状態: ブラウザ内の保存データを消した状態（またはプライベートウィンドウ）のデモモードで、本戦3チームに切替がなく、候補3チームのうち表示中が1と示され、entry-02 を ON にすると2になる。取込欄が画面から消えている
  - _Requirements: 1.2, 2.6, 3.1, 3.2, 3.3, 3.4, 3.5, 3.6, 5.7, 8.1, 9.2_
  - _Boundary: Admin Dashboard_

- [ ] 3.3 管理画面の集計を、自動更新・順位付き・同票の明示に作り直す
  - 入室後、チーム・票・受付状態を購読し、変化があるたびに集計パネルと受付ボタンを描き直す。表示切替パネルは、票の変化では描き直さない（チームの顔ぶれや名称が変わったときだけ組み直す）
  - オーディエンス賞候補を全体で1チームとして示す。最多が複数なら「同票」と該当チームをすべて示し、票が0件なら「未判定」と示す
  - 全体集計に総票数と、得票数の多い順の順位（同数は同順位、次の順位を飛ばす。同数内はNo順）を付ける。部門別集計も得票数の多い順に並べる。どのチームにも一致しない票は「集計対象外」として件数だけ示す
  - 完了状態: デモモードで、票を足すと順位と候補が変わり、2チームを同数にすると「同票」と両チームが表示され、票を空にすると「未判定」になる。表示中の候補を切り替えても、集計の数字は変わらない（表示状態にかかわらず全チームが集計対象）
  - _Requirements: 5.1, 5.2, 5.3, 5.4, 5.5, 5.6, 5.7, 8.2_
  - _Depends: 3.2_

- [ ] 3.4 (P) 投票APIに本戦の常時表示を反映し、設定漏れを失敗として返す
  - 表示判定を「本戦または表示ON」に揃える
  - DB URL か KV binding が欠けていれば、検証も保存もせずに設定漏れのエラーを返す。メモリ上のフォールバックを削除する
  - リクエストの形式エラーと、それ以外のエラーを区別して返す
  - 完了状態: ローカル起動した Worker に対して、不正ID→400、表示OFFの候補→403、本戦（表示状態を false にしても）→200で票が1件増える、同じトークン→409、再起動後も同じトークン→409、DB URL を外して起動→500 の設定漏れ、が確認できる（確認用のチームと票は確認後に削除する）
  - _Requirements: 1.2, 1.4, 4.5, 6.1, 6.2, 6.3, 6.4, 7.1, 7.2, 11.4_
  - _Boundary: Voting API_
  - _Depends: 1.2, 1.3_

- [ ] 4. 結合: 画面とAPIの通し確認、手順書、専用DBへの登録
- [ ] 4.1 テスト用のチームで、ローカル起動した Worker と専用DBを通しで確認する
  - 登録スクリプトにサンプル入力（本戦・候補・本戦で表示状態 false のチームを含む）を与えてパッチを作り、専用DBに書き込む。このとき、項目単位のパッチで、指定していない項目が消えないことを確認する（消える場合は、チーム単位で書き込む方法に切り替える）
  - デザイン刷新後の投票画面が読み込むリポジトリ内のファイルがすべて配信の許可リストに入っており、ブラウザで404が出ないことを確認する（足りなければ許可リストに追加する）
  - 管理画面で投票受付を開始する
  - 投票画面で表示対象のチームだけがNo順に出ることを確認する。表示状態 false の本戦チームが、投票画面・投票API・管理画面の表示数で、同じように「表示」として扱われることを確認する
  - 管理画面で候補を1件 ON にし、投票画面を再読み込みすると1チーム増えることを確認する
  - 管理画面を開いたまま、2つのブラウザ（別々の端末トークン）で投票し、再読み込みなしで総票数・順位・オーディエンス賞候補が更新されること、表示切替パネルのチェックの位置が動かないこと、同じブラウザの2回目が拒否されることを確認する
  - 投票受付を停止し、投票画面のフォームが無効になることを確認する
  - 一時的に `teams` への書き込みを拒否するルールを反映して切替を行い、チェックが元に戻ってエラーが表示されることを確認する（確認後、ルールを元に戻す）
  - 完了状態: 上記がすべて、認証なし・権限エラーなしで確認でき、最後に専用DBの `teams`・`votes` を空にし、ルールを本来のものに戻した状態で終わっている
  - _Requirements: 1.2, 2.3, 3.3, 3.4, 3.5, 3.6, 4.1, 5.1, 5.2, 5.6, 5.7, 6.4, 7.1, 8.1, 8.2, 8.3, 10.2, 13.2_
  - _Depends: 1.4, 2.1, 3.1, 3.3, 3.4_

- [ ] 4.2 (P) 手順書を、設計の運用に合わせて更新する
  - チームの登録・動画URLの追記・名称の修正の手順（審査アプリは読み取りのみ、プロジェクトIDは環境変数で渡す）を、新しい手順書にまとめる
  - デプロイ手順に、接続設定の置き方、Worker の secret、KV、配信の許可リスト、SPA の設定を外したこと、ルールの反映、当日のログの見方、公開後の確認項目（DB URL の一致、配信ファイル、審査アプリのルール）を反映する
  - README から、CSV/Excel取込の記述を削除し、ファイル構成と接続設定の説明を更新する
  - 完了状態: 設計に挙げた手順がすべて手順書にあり、リポジトリ内のどのファイルにも、審査アプリのプロジェクトID・URL・接続値が含まれない
  - _Requirements: 2.4, 10.3, 10.4, 11.3, 11.4_
  - _Boundary: Runbook_
  - _Depends: 1.4, 2.2_

- [ ] 4.3 本戦のNo一覧で、48チームを専用DBに登録する
  - 前提: 本戦12チームのエントリーNo一覧を、運営側から受け取っている
  - 前提確認: Firebase CLI で審査アプリのプロジェクトを読み取れる。読み取れない場合は、認証なしの読み取りリクエストで代わりに取得する（審査アプリのルールは全開放のため）
  - 専用DBの `teams` にテストや仮のデータが残っていないことを確認し、残っていれば空にする
  - 審査アプリのチーム一覧を読み取りのみで取得し、パッチを生成して専用DBに書き込む
  - 完了状態: 専用DBに48チームが登録され、本戦12チームが本戦・表示、36チームが候補・非表示になっている。審査アプリのルールが変わっていないことを、読み取りで確認できる
  - _Requirements: 2.1, 2.2, 2.4, 10.3_
  - _Depends: 1.2, 2.1_
  - _Blocked: 本戦12チームのエントリーNo一覧が未入手（運営側から受け取ったら、この行を削除して実行する）_

- [ ] 5. 本番環境の設定と、実機での確認
- [ ] 5.1 デプロイ先のアカウントに、KVとWorkerのsecretを用意する
  - 前提: デプロイ先の Cloudflare アカウントが確定し、この端末からそのアカウントにログインできる
  - 二重投票防止用のKV namespace を作成し、Worker設定の binding ID を本番のものに差し替える
  - Worker の secret に、専用DBのURL（接続設定の DB URL と同じ値）を設定する
  - 完了状態: アカウントのKV一覧に投票用の namespace があり、Worker の secret 一覧に DB URL がある
  - _Requirements: 7.2, 11.3, 11.4_
  - _Blocked: デプロイ先の Cloudflare アカウントが未確定（確定し、この端末からログインできたら、この行を削除して実行する）_

- [ ] 5.2 管理画面の入室ID・パスワードを変更する
  - 前提: ユーザーの指示で保留中。本番公開の直前に実施する
  - 手元の接続設定の入室ID・パスワードを、推測されにくい値に変更する
  - 完了状態: 初期値（admin/admin123）では入室できず、新しい値では入室できる
  - _Requirements: 9.1, 9.2_
  - _Blocked: ユーザーの指示で保留中（本番公開の直前に、この行を削除して実行する）_

- [ ] 5.3 本番にデプロイし、実機で確認する
  - 公開URLで、配信する7ファイルが取得でき、非公開のファイルが404になり、配信ファイルに審査アプリの文字列が含まれないことを確認する
  - 確認はブラウザのプライベートウィンドウで行う（当日使う端末に「投票済み」の記録を残さないため）
  - 別々の端末2台で投票して集計に反映されること、同じ端末の2回目が拒否されること、受付停止でフォームが無効になることを確認する
  - 投票画面を開いた後、Firebase の使用状況で接続数が戻ることを確認する
  - 審査アプリのルールが変わっていないことを、読み取りで確認する
  - 完了状態: 上記がすべて確認でき、確認用の票と、本番KVの確認用トークンの記録を削除し、票が0件・受付停止の状態で終わっている。来場者に配るQRコードの行き先が、最終的な公開URLと一致している
  - _Requirements: 4.5, 8.3, 9.1, 10.2, 10.4, 11.3, 13.1, 13.2_
  - _Depends: 4.2, 4.3, 5.1, 5.2_
  - _Blocked: 4.3・5.1・5.2 の完了待ち_

## Implementation Notes
- タスク1.4: `not_found_handling: "single-page-application"` を外す前の時点でも、`wrangler dev` では存在しないパスが既に404を返していた（design.mdの「SPA設定ありだとindex.htmlが200で返る」という前提の記述と食い違う）。`worker.js` が `env.ASSETS.fetch()` にフォールバックしており、ローカルの `wrangler dev` ではアセット未マッチ時にプラットフォームのSPAフォールバックが作動していなかった可能性がある。設定削除自体はdesign.mdの明示的な指示（クライアント側ルーティングを使わない）どおりで正しく、変更後の完了条件（配信対象200・非対象404）は満たされている。本番Cloudflare環境での挙動もタスク5.3の実機確認で併せて確認すること。
